info:
  name: Replace post
  type: http
  seq: 4

http:
  method: PUT
  url: "{{host}}/posts/1"
  body:
    type: json
    data: |-
      {
        "userId": 1,
        "title": "new title",
        "body": "new text"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
