info:
  name: Fetch posts
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/posts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
