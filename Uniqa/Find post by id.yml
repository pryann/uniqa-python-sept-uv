info:
  name: Find post by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/posts/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
