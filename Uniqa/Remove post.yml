info:
  name: Remove post
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/posts/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
